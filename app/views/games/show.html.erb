<%= @game.name %>
<%= @game.owner.username %>

<% unless @game.owner == current_user %>
  <%= link_to "Ask for a trade", new_trade_path(asker: current_user,
                                                receiver: @game.owner,
                                                game: @game) %>
<% end %>
