<div style="padding-top: 70px;">
  <%= @game.name %>
  <%= @game.owner.username %>

  <% unless @game.owner == current_user %>
    <%= link_to "Ask for a trade", game_trades_path(@game), method: :post %>
  <% end %>
</div>
